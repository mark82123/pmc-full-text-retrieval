# PMC 全文檢索工具 — 課堂講稿(約 8 分鐘)

作者:胡元楨 · 學號 P77141105 · Project #1 for the AI Information Retrieval Course(人工智慧資訊檢索)

> 投影:一頁式簡報 + 網站 http://172.27.156.215:8090/
> 課前:執行 `python3 app.py --host 0.0.0.0 --port 8090`,先貼一個 PMID 試抓確認網路可連 NCBI。

---

## 0. 開場(30 秒)

大家好,我是胡元楨。這是專案一:一個從零寫的 PubMed Central 全文檢索引擎。
整個專案只用 Python 標準庫,沒有安裝任何套件;斷詞、詞幹化、倒排索引、BM25 排序、句子切分全部自己實作。

今天分三段:先看整體流程,再講前處理和用到的演算法,最後現場示範。

## 1. 整體流程(1 分鐘)— 指著簡報上方六個方塊

系統分成兩半。

**左邊藍綠色是後端,收錄資料時做的事,使用者看不到:**
1. 取得與解析:輸入 PMID 或 PMC ID,系統向 NCBI 抓 JATS XML,解析出標題、摘要、內文段落、作者、期刊。
2. 前處理:斷詞、小寫化、停用詞移除、Porter 詞幹化,等一下細講。
3. 索引:建成位置倒排索引,除了「哪些文件含這個詞」,還記錄「出現在第幾個位置」和「屬於標題、摘要還是內文」。

**右邊琥珀色是前端,使用者每次送出查詢時即時做的事:**
4. 查詢:支援 AND / OR / NOT、括號、"片語"、prefix* 萬用字元、title: / abstract: / body: 欄位限定。
5. 排序:預設 Okapi BM25,也有 TF-IDF;可以整篇評分,也可以只用摘要評分。
6. 呈現:反白片段、命中位置圖、規則式句子切分。

兩邊的交會點就是倒排索引:後端把它建好,前端拿它來查。

## 2. 前處理(2 分鐘)— 指著簡報左下大卡

前處理是把原文變成索引詞的四個步驟,每一步都有取捨。

**第一步,斷詞。** 規則只有一條:連續的字母或數字是一個 token,其他字元都是分隔符。所以 COVID-19 變成 covid 和 19,T-cell 變成 t 和 cell。
好處是生醫文獻的連字號用法很亂,T-cell、T cell 都能互相匹配;代價是像 1,024 這種數字會被切開。

**第二步,小寫化。** Cancer、cancer、CANCER 視為同一個詞。
副作用是大小寫承載的語意會消失:WHO 變成 who,正好是停用詞被丟掉;SARS 變成 sars,再被詞幹化砍成 sar。

**第三步,停用詞移除。** 用的是 Snowball 的經典 174 字清單。
我另外加了 17 個生醫論文特有的高頻詞,方法是掃描語料的 document frequency,只留沒有主題意義的詞:et 和 al 來自 et al.,每篇論文出現幾十次卻毫無鑑別力;however、may、therefore 是學術寫作的語氣詞。
一個細節:停用詞移除後位置編號仍然保留,所以片語 "role of the gut" 索引成 role 在第 0 位、gut 在第 3 位,片語查詢照樣能比對。

**第四步,Porter 詞幹化。** 這是照 Porter 1980 年論文的五個步驟自己寫的,不是呼叫 NLTK。
immunotherapies 和 immunotherapy 都變成 immunotherapi,probiotics 變成 probiot。
它是純規則、不查字典,所以有已知的失誤:organization 和 organ 會被併成同一個詞幹,這叫 over-stemming;analysis 和 analyses 反而變成兩個不同詞幹,這叫 under-stemming。網站的「前處理與演算法」分頁可以輸入任何字看每一步的變化。

## 3. 導入的演算法(1 分鐘)— 指著簡報右下深色卡

- **位置倒排索引**:term 對應到「文件 → 位置清單」,是片語查詢和欄位限定的基礎。
- **布林檢索模型**:查詢用遞迴下降解析器處理,優先序 NOT 高於 AND 高於 OR。
- **Okapi BM25**:k1 = 1.5、b = 0.75。詞頻會飽和,長文件會被正規化,稀有詞權重高。也提供 TF-IDF 做對照。
- **分欄評分**:每篇文件同時算整篇、標題、摘要、內文四個分數,欄位分數用該欄位自己的長度做正規化,所以摘要跟摘要比。老師提到檢索針對摘要,這裡可以切換只用摘要排序。
- **規則式句子切分**:縮寫表、人名首字母、屬名 E. coli、小數點都不切。天真法只看句號,在生醫論文會多算兩到三成的句子。
- **NCBI 整合**:PMID 先轉 PMCID,再用 efetch 抓全文;沒有全文的文章改抓 PubMed 摘要建索引。

## 4. 現場示範(3 分鐘)

### 4a. 建庫(切到 Add Documents 分頁)

貼入記事本的 PMID,按 Fetch & index。

```
36043345
39679259
40104324
40661744
39118728
39044427
27741350
```

講:「前六篇是 PMC 開放取用全文;最後一篇 27741350 在 PMC 沒有全文,系統自動改抓 PubMed 摘要,狀態表標成 abstract only。」

若時間夠,再貼三篇 COVID:

```
35308540
39703784
33428981
```

### 4b. 檢索(切到 Search 分頁)

依序輸入,每個講一句:

| 查詢 | 講什麼 |
|---|---|
| `gut microbiota` | 預設 AND,兩個詞都要出現。看查詢分析列:每個詞的詞幹和 df。 |
| `"gut microbiota"` | 片語查詢,靠位置索引。 |
| `"microbiota gut"` | 詞序反過來,只剩命中 microbiota-gut-brain axis 的文件,證明是真的看位置。 |
| `microbiota NOT cancer` | 布林 NOT。 |
| `title:crispr` | 欄位限定,只看標題。 |
| `probiotics` 再輸入 `probiot*` | 查詢分析列顯示 probiotics 被詞幹化成 probiot;萬用字元套在詞幹上。 |
| `title:evolution` | 只命中 abstract-only 那篇,證明摘要也能檢索。 |
| `covid AND microbiota`(若抓了 COVID 三篇) | 新舊語料的交集只有一篇。 |

指著結果卡右側:「命中數分成 title、abstract、body 三列,右邊的柱狀圖是命中落在該欄位的哪個位置。」

把 Rank by 切到 Abstract only,講:「同一個查詢只用摘要評分,名次會變,摘要沒有命中的文件會消失。」

### 4c. 文件檢視與句子切分

點一篇標題進入文件頁,勾 Show sentence boundaries。
講:「規則式切出的句數和天真法並列,差距來自 et al.、Fig. 2、E. coli 這些帶句號的寫法。」

## 5. 收尾(30 秒)

總結三句:
1. 純標準庫從零實作,前處理與演算法都能逐步解釋。
2. 位置倒排索引讓片語、欄位限定、分欄評分成為可能。
3. 已知限制:Porter 的 over / under-stemming、縮寫與停用詞衝突、不規則複數;解法是 lemmatization,但那需要詞典與模型。

謝謝大家。

---

## 可能被問的問題

**Q:為什麼不用 NLTK 或 scikit-learn?**
課程要求從零實作,而且自己寫才能解釋每一步。網站「前處理與演算法」分頁最後有一張表,列出 NLTK、snowballstemmer、scikit-learn、spaCy 各自對應的功能,本專案沒有 import 任何一個。

**Q:生醫補充停用詞怎麼挑的?**
原始清單是人工整理,之後用語料的 document frequency 掃描驗證:例如 however 出現在 10 篇中的 10 篇、may 9 篇、also 8 篇,和內容詞 patient 的 6 篇對照。掃描與整理的過程有用 LLM 協助。原本清單裡的 us 和 shall 因語料中幾乎不出現、且 us 會誤傷 U.S. 而移除。

**Q:BM25 的 k1 和 b 是什麼?**
k1 控制詞頻飽和的速度,b 控制文件長度正規化的強度,用的是常見預設值 1.5 和 0.75。

**Q:分欄評分和 BM25F 一樣嗎?**
概念相近但更簡單:BM25F 是把各欄位的詞頻加權後合併成一個分數;本系統是每個欄位各算一個獨立的 BM25,讓使用者選擇用哪個排序。

**Q:SARS-CoV-2 這種詞怎麼查?**
斷詞會切成 sars、cov、2,系統自動把帶連字號的查詢當成相鄰片語比對。要注意萬用字元 sars* 會失敗,因為 sars 的詞幹是 sar。

**Q:句子切分的縮寫表有多大?**
約 90 個,包含 e.g.、i.e.、et al.、Fig.、Dr.、vs.、月份縮寫、單位縮寫等,定義在 ir/sentence.py。
